[Voltar ao indice](../README.md)

### Exemplo de prompt (Chain-of-Thought)
Caso de uso: quando a tarefa parece simples, mas voce quer reduzir erro de interpretacao tornando o raciocinio verificavel. Neste exemplo, o modelo explicita como identifica colunas e mapeia valores antes de responder.

Entrada:
```code-block
Extraia os dados do CSV e retorne nome, sobrenome e matricula.
Pense passo a passo antes de retornar o resultado.

Exemplo

Entrada:
nome,sobrenome,matricula
Geo,Cavalcante,123

Raciocinio:
1. Identifico que o CSV possui 3 colunas: nome, sobrenome e matricula
2. Leio a primeira linha de dados: Geo,Cavalcante,123
3. Mapeio cada valor para sua coluna correspondente
4. Formato a saida com os campos identificados

Saida:
nome: Geo, sobrenome: Cavalcante, matricula: 123

Agora processe esta entrada do arquivo anexado.
Mostre seu raciocinio passo a passo antes da resposta final.
```

### Diagrama de Fluxo

```mermaid
flowchart TD
    A[Entrada: CSV a ser processado] --> B[Prompt: instrucao com exemplo e pedido de raciocinio]
    B --> C[Decompor a tarefa em etapas]
    C --> D[Passo 1: identificar colunas]
    D --> E[Passo 2: ler a linha de dados]
    E --> F[Passo 3: mapear valores para campos]
    F --> G[Passo 4: formatar a resposta]
    G --> H[Aplicar a mesma sequencia ao caso atual]
    H --> I[Saida final: raciocinio explicito e resposta]

    classDef input fill:#2563eb,stroke:#1e3a8a,color:#fff
    classDef prompt fill:#475569,stroke:#334155,color:#fff
    classDef technique fill:#d97706,stroke:#92400e,color:#fff
    classDef reasoning fill:#7c3aed,stroke:#5b21b6,color:#fff
    classDef action fill:#0f766e,stroke:#115e59,color:#fff
    classDef observation fill:#0891b2,stroke:#0e7490,color:#fff
    classDef decision fill:#dc2626,stroke:#991b1b,color:#fff
    classDef output fill:#16a34a,stroke:#166534,color:#fff

    class A input
    class B prompt
    class C,D,E,F,G reasoning
    class H action
    class I output
```

> **Caracteristica:** O modelo e instruido a pensar passo a passo, exibindo o raciocinio intermediario antes da resposta final. Reduz erros em tarefas que exigem logica sequencial.
